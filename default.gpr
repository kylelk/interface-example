project Default is
  for Object_Dir use "obj";
   for Source_Dirs use ("src", "src/json_parser");
   package Compiler is
      for Default_Switches ("ada") use ("-g", "-O2", "-gnat2012");
   end Compiler;

   for Main use ("main.adb");
end Default;

